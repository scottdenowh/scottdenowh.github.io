{
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "postinstall": "patch-package",
    "firebase": "firebase",
    "deploy": "firebase deploy --only hosting,functions --project osd-admin-portal"
  },
  "dependencies": {
    "firebase": "^12.11.0",
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.2.2"
  },
  "devDependencies": {
    "firebase-tools": "^15.11.0",
    "patch-package": "^8.0.1"
  }
}
